# khr-quadruped19 (v19) 実機動作確認の記録（動画からの調査）

- 記録日: 2026-08-10
- 資料: `/home/tora/gsw4/IMG_7791.mp4` / `IMG_7792.mp4` / `IMG_7793.mp4`（2026-08-10 20:32、1280×720 @30fps）
  - IMG_7791: 8.6 秒（接近する近景カット）
  - IMG_7792: 62.2 秒
  - IMG_7793: 62.6 秒
- 調査方法: ffmpeg でフレーム抽出（全体コンタクトシート／歩容の連続フレーム／足元の拡大）。

## 1. 何が動いていたか（構成の確認）

`Meridian_consoleS/Meridian_consoleS_quad.py`（2026-08-07 更新）の設定より:

```python
log_dir  = "../khr_rl_tora/logs/"
exp_name = "khr-quadruped19"   # ← v19
ckpt     = 3999
```

→ **動画は v19（khr-quadruped19, model_3999）を実機で実行したもの**と確認できる。

## 2. 動画から読み取れたこと

### ✅ 四足歩行が実機で成立している
- 3 本すべてで、KHR-3HV が**前のめり四足姿勢**（腕＝前脚 / 脚＝後脚）を保って**床上を移動**している。
- **62 秒間の連続動作で転倒なし**。両方の長尺動画とも、最終フレームまで四足姿勢を維持している。
- 位置・向きが継続的に変化しており、その場足踏みではなく**実際にロコモーションしている**。

### ✅ 後脚の足裏が「全面接地」している（v18〜v19 の成果が実機で再現）
- 足元を拡大したフレームで、**後脚の大きな足裏プレートが床に対して平らに接地**しているのが確認できる。
- **つま先だけ／踵だけで立つ場面は観察されなかった**。
- v19 のシミュ実測（後脚 足裏傾き 接地時平均 **8.4°**）と矛盾しない挙動であり、
  ユーザ要望「足の裏全体を使って歩く」が**実機でも成立している**と判断できる。

### ✅ 符号キャリブレーション問題は顕在化しなかった
- `genesis2khr_dir` は**依然として全 +1（未校正）**のままだが、実機は破綻せず歩いている。
- ⇒ **URDF（Genesis）と実機サーボの回転方向が概ね一致していた**ことを意味する。
  `RealRobotVerification.md` で「実機化の最大の壁」と位置づけていたブロッカーは、**実際には障害にならなかった**。

## 3. 現時点で分かっていないこと（データ欠落）

**実行ログが一切保存されていない。**
`Meridian_consoleS` / `khr_rl_tora` 配下に、当日の CSV・ログファイルは見つからなかった
（`obs2csv` は未使用）。そのため以下は**未測定**である。

| 項目 | 状態 |
|---|---|
| 実機の歩行速度 | **未測定**（動画からの目視のみ。シミュは 0.244 m/s） |
| Sim-Real の観測ギャップ | **未測定**（`obs2csv` を使えば取得可能） |
| 実機のサーボ負荷・発熱 | **未測定**（シミュのピークは定格 81%） |
| 指令（vx/vy/wz）と実挙動の対応 | **不明**（どの方向指令を出していたかログが無い） |
| 通信品質（UDP エラー率・周波数） | **不明**（コンソールの Message 表示は動画に写っていない） |

※ 本記録の「速度」「傾き」に関する記述は**動画からの定性的観察**であり、計測値ではない。

## 4. 現状の評価

**Sim2Real の第一関門は突破できている。**
v1〜v19 でシミュ上に構築した四足歩行ポリシーが、**実機で転倒せず 60 秒以上歩き続け、
かつ足裏全面での接地というシミュ上の設計意図まで再現**した。
懸案だった符号キャリブレーションも実質的に問題にならなかった。

一方で、**定量的な検証はこれからの段階**である。現状は「動いた」ことの確認にとどまり、
シミュとのギャップを数値で示す材料が無い。卒業論文で Sim2Real を主張するには、
次項の計測が必要になる。

## 5. 次にやるべきこと（優先度順）

1. **`obs2csv` を有効にして再実行し、実機観測を記録する**（最優先）。
   シミュの観測（同一コマンド・同一 ckpt）と重ねれば、**Sim-Real ギャップを定量化**できる。
   sim2real ウィンドウの「obs2csv」ボタンで取得できる。
2. **実機の歩行速度を実測する**（床にテープで既知距離を作り、通過時間を計測）。
   シミュの 0.244 m/s と比較する。ギャップの最も分かりやすい指標になる。
3. **サーボの発熱を確認する**（連続歩行後に手で触れて確認、可能なら温度計）。
   シミュのピークは定格 81% であり、実機で許容範囲かを確かめる。
4. **指令別の動作確認**（前進・後退・横・旋回を個別に指令し、それぞれ撮影＋ログ）。
   現在の動画は指令内容が不明なため、方向別の追従を評価できていない。
5. 通信品質（エラー率・周波数）の記録。コンソールの Message ウィンドウを撮影するだけでも可。
