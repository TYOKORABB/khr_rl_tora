# 開発日記 — KHR-3HV 四足歩行の強化学習（v1〜v17）

> 卒業論文の一次資料として使うことを想定した、構造化された開発記録。
> 各バージョンについて「動機 → 変更点 → 実行方法 → 結果 → 分析 → 次への課題」を
> 一貫した粒度で記述している。数値は `experiments/khr-quadruped*/report.md`、
> `experiments/khr-quadruped8/videos.md`、各 `khr_train_quad*.py` の冒頭 changelog、
> および評価スクリプトの実測に基づく。

- 対象ロボット: 近藤科学 **KHR-3HV**（小型二足ヒューマノイド、22 関節）
- 最終目標: Genesis で学習した四足歩行ポリシーを **Meridian 経由で実機へ Sim2Real 転移**
- 本記録の範囲: シミュレーション内の四足歩行獲得（v1〜v17）。実機転移は準備段階（後述）
- 作成日: 2026-07-24

---

## 目次

1. [研究の位置づけと全体設計](#1-研究の位置づけと全体設計)
2. [システム・アーキテクチャ](#2-システムアーキテクチャ)
3. [実行方法（コマンド集）](#3-実行方法コマンド集)
4. [バージョン別 開発日記（v1〜v17）](#4-バージョン別-開発日記v1v17)
5. [全バージョン横断の実測比較](#5-全バージョン横断の実測比較)
6. [重要な失敗と学び（トラブルシューティング）](#6-重要な失敗と学びトラブルシューティング)
7. [残課題と今後の方針](#7-残課題と今後の方針)
8. [付録](#8-付録)

---

## 1. 研究の位置づけと全体設計

### 1.1 背景と目的

本研究は、先輩が実装した **KHR-3HV の二足歩行 RL パイプライン**（Genesis + rsl-rl、
および実機動作）を土台に、**同じ機体で四足歩行を新規に獲得する**ことを目的とする。
二足ではなく四足を選ぶことで、より安定した歩行基盤の上で Sim2Real の成功率を検証する。

- ベースリポジトリ: `khr_rl_sample`（先輩由来、二足歩行）を fork した `khr_rl_tora`
- 二足版の中核 `khr_env.py` / `khr_train.py` は**触らず**、四足専用に
  `khr_quad_env*.py` / `khr_train_quad*.py` を新規作成して開発した。
- **ファイル分離の原則**: バージョンごとに env / train / record を複製し、番号を増やす。
  過去バージョンの学習結果 (`logs/`)・研究記録 (`experiments/`)・モデルには一切触れない。
  実機での検証がまだ済んでいないため、資産を完全に分けて追跡可能性を保つ方針。

### 1.2 技術スタック

| 要素 | 内容 |
|---|---|
| シミュレータ | Genesis (`genesis-world` 0.4.6, editable install) |
| RL ライブラリ | rsl-rl-lib ≥ 5.0（現 5.4.0）, PPO, `OnPolicyRunner` |
| DL フレームワーク | PyTorch 2.12.0, tensordict |
| 制御周期 | 50 Hz（`dt = 0.02`）。実機 Meridian は 100 Hz、ポリシーは 50 Hz |
| バックエンド | 学習・録画は GPU（`gs.gpu`）、評価ビューアは CPU |

### 1.3 四足形態の設計思想

二足ヒューマノイドを四足化するための機構的な工夫:

- **腕 = 前脚、脚 = 後脚**。URDF を `khr3hv_12dof.urdf`（腕 fixed）から
  `khr3hv.urdf`（腕 revolute）に変更し、肩・肘を可動にして前脚として使う。
- **胴体を y 軸まわりに +90° ピッチ**させ、「前のめりの四足姿勢」を初期姿勢とする。
  （初期の試作では −90°=後ろ倒れだったが、見た目・心理面から前倒れに変更。理由は §6.2）
- 制御対象は**全 22 関節**（腕 8 + 脚 12 + 胴/頭 yaw 2）。`num_actions = 22`。
- 接地点は 4 点: 前脚 `l_lowerarm` / `r_lowerarm`、後脚 `l_foot` / `r_foot`。
- 歩容は **4 脚トロット（対角ペア）**。対角の (FL,RR) と (FR,RL) を位相 0.5 ずらす。

---

## 2. システム・アーキテクチャ

### 2.1 ロコモーション座標系補正（本研究で最も重要な工夫の一つ）

胴体を 90° 倒しているため、**ベース座標系の各軸が世界座標とずれる**
（base-z = 世界前方、base-x = 世界下方）。速度追従・姿勢報酬をベース座標系のまま
計算すると「前進を罰する」等の座標系バグが起きる。これを防ぐため、報酬計算では
初期姿勢クォータニオンで世界と揃えた **loco frame** に変換する。

```python
self.loco_lin_vel = transform_by_quat(self.base_lin_vel, self.init_base_quat)
self.loco_ang_vel = transform_by_quat(self.base_ang_vel, self.init_base_quat)
projected_gravity_rel = transform_by_quat(self.projected_gravity, self.init_base_quat)
```

- 速度・コマンド・姿勢報酬 → **loco frame** で評価。
- 観測の `base_ang_vel` / `projected_gravity` → **base 座標系のまま**残す。
  （実機の IMU=ジャイロ/加速度計は機体座標で測るため、Sim と Real を揃える）
- この非対称が Sim2Real 整合の要。評価スクリプトで速度を測るときも `loco_*` を使う
  必要がある（base 座標のまま測ると旋回が −4% と誤って出る。§4.9 の実測時に遭遇）。

### 2.2 観測（`num_obs = 121`）

Actor が受け取る観測（`_update_observation`）:

| スライス | 内容 | 次元 | 座標系 |
|---|---|---|---|
| [0:3]    | base 角速度 | 3 | base |
| [3:6]    | 投影重力 (projected gravity) | 3 | base |
| [6:9]    | コマンド (vx, vy, wz) × スケール | 3 | loco |
| [9:31]   | 関節角の default からの偏差 | 22 | — |
| [31:53]  | 関節角速度 | 22 | — |
| [53:75]  | 現在の action | 22 | — |
| [75]     | cos(phase) | 1 | 歩容位相 |
| [76]     | sin(phase) | 1 | 歩容位相 |
| [77:99]  | 前ステップの action | 22 | — |
| [99:121] | 前ステップの関節角速度 | 22 | — |

### 2.3 特権観測（critic 用、`num_privileged = 80`）— 非対称 Actor-Critic

critic だけが観測できる真値（ノイズなし＋DR パラメータ）を与える。

`base_ang_vel(3) + projected_gravity(3) + dof_pos偏差(22) + dof_vel(22) + last_dof_vel(22)
+ loco_lin_vel(3) + 追加質量(1) + 摩擦(1) + COMシフト(3) = 80`

- `obs_groups`: actor → `policy`、critic → `policy` + `privileged`。
- loco 系の並進速度・DR パラメータは実機で観測不能なので actor には渡さない。

### 2.4 制御と歩容

- **PD 位置制御**: `kp = 25`, `kd = 0.5`, `action_scale = 0.15`。
  22 関節を `motors_dof_idx`（各関節の `dof_start`）で**インデックス指定バッチ制御**。
  DOF の内部順とコンフィグの関節順が一致しないため、名前→インデックスで明示的に対応付ける。
- **1 ステップの action 遅延**（`simulate_action_latency=True`）— 実機の遅延を模擬。
- **トロット位相**: `gait_period = 0.5 s`、`gait_offsets = [0.0, 0.5, 0.5, 0.0]`（FL,FR,RL,RR）。

### 2.5 報酬関数（`_reward_<name>` を自動探索）

| 報酬項 | 役割 |
|---|---|
| `tracking_lin_vel` | 指令並進速度への追従（**最重要**、v3 で 1.5→4.0） |
| `tracking_ang_vel` | 指令旋回速度への追従 |
| `orientation` | 胴体姿勢を公称に保つ |
| `base_height` | 目標高さの維持 |
| `lin_vel_z` / `ang_vel_xy` | 上下動・不要な回転の抑制 |
| `gait_contact` / `gait_swing` | トロット位相に合った接地/遊脚を促す |
| `contact_no_vel` | 接地脚の滑りを罰する |
| `feet_clearance` | 遊脚を目標高さまで持ち上げる（すり足抑制） |
| `hip_pos` | 脚の横開き（roll/yaw）を罰する。**v8 で hip_yaw/ankle_roll を追加** |
| `feet_orientation` | 足裏を水平に保つ（**v7 で新設**） |
| `alive` | 生存ボーナス |
| `action_rate` / `dof_vel` / `acceleration` / `joint_torques` | 動作の滑らかさ・省トルク |
| `collision` | 自己衝突の罰 |

報酬 scale は init 時に `dt` を一度だけ掛ける。scale と `_reward_` メソッドは名前で
1:1 対応が必須（片方だけあると壊れる）。

### 2.6 Sim2Real レシピ（ドメインランダム化・ノイズ）

先輩の実機二足で実証済みのレシピを v1 から継承:

- **ドメインランダム化**: 摩擦 [0.1,1.5]、追加質量 [−0.1,0.5]kg、COM シフト [−0.02,0.02]m、
  kp スケール [0.9,1.1]、kd スケール [0.8,1.2]。
- **観測ノイズ**: 角速度・重力・関節角・関節速度に加算（実機センサ誤差の模擬）。
- **1 ステップ action 遅延**（前述）。
- **push 外乱**: 学習中に胴体へランダム水平速度キックを与え転倒回復を学ぶ
  （v5 で有効化、v6 でカリキュラム化）。

### 2.7 URDF・関節の物理制約（設計を縛る重要事実）

- **サーボ定格トルク 1.373 Nm**（実機 KONDO サーボ）を URDF の effort limit に設定。
  Genesis がこれを強制するため、飽和すると Sim でも指令通り動けない＝実機で発熱・脱調。
- **膝は片方向のみ**: `knee_pitch` の可動域は **[0.000, 2.793] rad**（負に曲がらない）。
  これが v8 で判明する「膝ロック」問題の物理的根源（§4.10）。
- 足裏水平の幾何条件: `hip_pitch + knee_pitch + ankle_pitch = −θ_base = −1.5708`。
  初期姿勢を作り直すときは常にこの条件を満たす。

---

## 3. 実行方法（コマンド集）

すべて venv 有効化後、`khr_rl_tora/` 直下から実行（URDF 参照が相対パスのため）。
GPU が必要（学習・録画）。録画は Chrome Remote Desktop/VS Code Remote 環境で
ビューアが見えないため、オフスクリーンで MP4 に保存する方式を採る。

```bash
source /home/tora/gsw4/bin/activate
cd /home/tora/gsw4/khr_rl_tora

# 学習（例: v8）。logs/khr-quadruped8/ に checkpoint と TensorBoard を出力
python khr_train_quad8.py -e khr-quadruped8 -B 4096 -I 4000 --seed 1

# 評価（ビューア。CPU）
python khr_quad_eval.py -e khr-quadruped8 --ckpt 3999

# 録画（ヘッドレス、MP4）。録画には X ディスプレイの指定が要る
export DISPLAY=:1
python khr_quad_record5.py -e khr-quadruped8 --ckpt 3999 --vx 0.4 --seconds 10 -o walk_v8_forward.mp4
#   方向指定: --vx 前進 / --vy 横 / --wz 旋回（未指定成分は 0）

# 研究記録の自動生成（report.md + metrics.csv を experiments/<exp>/ に出力）
python khr_quad_report.py -e khr-quadruped8

# 学習曲線
tensorboard --logdir logs
```

- **注意**: `khr_train_quad*.py` は同名 `exp_name` の `logs/<exp>/` を実行開始時に削除して
  作り直す。残したい結果は別 exp 名にするか事前に退避する。
- **録画スクリプトの世代対応**: `khr_quad_record.py` は v1 env を import する。v8 は
  `khr_quad_record5.py`（`khr_quad_env5` を読む複製）を使う。
- **`export DISPLAY=:1` が必須**: シェルで `DISPLAY` 未設定でも `/tmp/.X11-unix/X1` が
  存在するため、これを指定しないと録画が "No display" で失敗する。

---

## 4. バージョン別 開発日記（v1〜v17）

各バージョンは env / train を複製して番号を増やし、直前版からの差分のみを変える
（ablation を明確化するため）。共通の固定値: `num_envs=4096`, `seed=1`,
PPO `[128,64,32]` ELU, `kp=25/kd=0.5`, `action_scale=0.15`, `init_std=0.5`,
`entropy_coef=0.01`, `lr=1e-3`, `gamma=0.99/lam=0.95`。

### 4.1 v1 — 初の四足歩行成功（`khr-quadruped`, 3000 iter）

- **動機**: 二足版 `khr_env.py` を四足化し、まず「転ばずに前進する」ことを成立させる。
- **主な実装**: `khr_quad_env.py` を新規構築（22DOF、トロット、loco frame 補正、121 次元観測、
  非対称 actor-critic）。コマンドは前進のみ `vx ∈ [0.0, 0.2]`。
- **初期のつまずきと修正**: 最初の run はエピソード長 12.5 のまま伸びず**完全失敗**。
  探索 std が早期崩壊していたため、`init_std 1.0→0.5`, `action_scale→0.15`,
  `entropy_coef→0.01` に緩めて**探索崩壊を回避**（commit `e8d346b`）。
- **結果**: エピソード長 12.5 → 1001（転ばず立てる）。前進追従報酬 最終 1.45。
  ただし **実測速度 約 0.036 m/s vs 指令 0.15**＝**すり足で非常に遅い**。
- **分析 / 次への課題**: 歩けたが遅い。速さと自然さの改善が必要。

### 4.2 v2 — 「速く・自然に」の試み（設定 3 箇所のみ、失敗から学ぶ）

- **動機**: 新報酬を足さず既存パラメータ調整だけで v1 の遅さ・すり足を改善。
- **変更点**: `tracking_sigma 0.25→0.15`（追従を厳しく）、`vx [0,0.2]→[0,0.4]`、
  `feet_height_target 0.035→0.06`（遊脚を高く）。
- **結果**: **かえって遅くなった**（前進追従 最終 1.12、v1 の 1.45 より低下）。
- **分析（重要）**: 追従判定を厳しくしても速くならなかった。報酬内訳を精査すると
  **静止でも前進追従報酬の約 94% が得られる**一方、動くと罰（acceleration /
  similar_to_default / action_rate）が増え「**歩く報酬 < 歩く罰**」となり、
  静止が最適解になっていた（探索は潰れていない＝報酬設計の問題）。
- **次への課題**: 報酬「バランス」の是正。厳しさではなく割の合う設計に。

### 4.3 v3 — 報酬バランス是正（ブレークスルー）

- **動機**: v2 の診断に基づき「歩くこと」を明確に割に合わせる（設定 4 箇所のみ）。
- **変更点**: `tracking_lin_vel 1.5→4.0`、`similar_to_default −0.1→−0.02`、
  `action_rate −0.05→−0.02`、`acceleration −1e-4→−2e-5`（実効最大の罰を 1/5）。
  sigma=0.15 / command[0,0.4] / feet_height=0.06 は v2 を維持。
- **結果**: **前進追従報酬 最終 3.89（v1 の 2.7 倍）**。指令 0.3 に対し実速度 0.257 m/s
  で**追従 86%**。「実際に速く歩く」四足が完成。
- **先行研究の裏付け**: Rudin et al. 2021 (legged_gym) の ablation で速度追従報酬が
  最重要と示されており、`tracking_lin_vel` の増強が効いたことと整合。
- **分析 / 次への課題**: 前進は解決。次は「コマンドで自在に歩ける」全方向化へ。

### 4.4 v4 (1a) — 全方向歩行への拡張

- **動機**: Sim2Real に向け、まず横移動・旋回を含む「自在に歩ける四足」に仕上げる。
- **変更点**: `command_cfg` のみ。`vy [0,0]→[−0.15,0.15]`、`wz [0,0]→[−0.5,0.5]`。
  `vx [0,0.4]` は据え置き（変更を「横・旋回の追加」だけに限定し ablation を明確化）。
  報酬・環境は v3 のまま（環境は既に x/y と yaw を扱えるためコマンド範囲を開くだけで学習可）。
- **結果**: 全方向歩行を獲得。前進追従報酬 最終 3.91。前進追従 ≈86%、**横 ≈77%**。
  足裏の傾き 12.4°、トルク定格 95%超に張り付く関節 2/22。**以降の比較基準（ベースライン）**。
- **分析 / 次への課題**: 押し外乱に対する頑健性は横 −y が最弱（0.8 で耐え 1.0 で全滅）。
  実機に向けて外乱耐性を上げたい（→ push 外乱の導入）。

### 4.5 v5 (1b) — push 外乱で頑健化（頑健性↔性能のトレードオフを観測）

- **動機**: 先輩 config に枠だけあって未実装だった push を有効化し、実機の外乱耐性を上げる。
- **実装**: 環境を **`khr_quad_env2.py`** に変更し `_push_robots()` を追加。
  一定間隔で胴体の水平速度にランダムキックを加える。
  `push_interval_s 5→3`、`max_push_vel_xy 0.2→1.0`、push モードを有効化。
- **結果**: 前方・横 +y の押し耐性を +0.5 m/s 改善。しかし**歩行追従が低下**
  （前進 93→69%、横 78→35%、旋回 87→64%）、後方耐性はやや悪化、最弱の横 −y は不変。
- **分析**: **最初から最大 1.0 m/s の強い外乱**を与えたため方策が「守り」に入り歩行が鈍った。
  典型的な**頑健性↔性能のトレードオフ**。
- **次への課題**: 外乱を徐々に強めるカリキュラムで両立を狙う。

### 4.6 v6 (1b) — push カリキュラム（頑健性と性能の両立）

- **動機**: v5 のトレードオフを、外乱をカリキュラムで漸増させて解消する。
- **実装**: 環境を **`khr_quad_env3.py`** に変更。`push_curriculum_steps=50000` を追加し、
  `push_scale = min(1.0, step_counter / curriculum_steps)` で押し強度を 0→max に線形増加。
  まず歩行を習得させ、その後で押しを強める。押し間隔・最大強度 (3s, 1.0m/s) は v5 のまま。
- **結果**: 前進追従報酬 最終 3.49。歩行性能を保ちつつ外乱耐性を回復（v5 の劣化を解消）。
- **分析 / 次への課題**: 頑健性は改善したが、v4→v6 の過程で**足の動きが不自然**に。
  「足の縁で歩く」挙動が悪化していることが実測で判明。

### 4.7 v7 — 足裏の水平接地（ベタ足歩行）

- **動機**: v4→v6 で足裏が傾き「縁で歩く」挙動が悪化していた。実測で
  足裏傾き v4 12.4° → v5 22.7° → v6 24.3°、`ankle_pitch` −6.3→−34.4°、
  `ankle_roll` −12.6→−29.7°、`hip_yaw` +6.0→+20.0°（脚が外にねじれる）。
  これは push に耐える「踏ん張り姿勢」に流れた副作用で、`ankle_roll`/`hip_yaw` が
  **定格トルク 1.373Nm を連続使用**する原因＝実機の発熱・脱調リスクでもあった。
- **実装**: 環境を **`khr_quad_env4.py`** に変更し `_reward_feet_orientation` を新設
  （足裏法線の水平成分を罰する＝水平なら 0）。`reward_scales` に
  `"feet_orientation": -1.0` を追加。push カリキュラムは v6 のまま維持。
- **結果**: 足裏傾きが大幅改善（≈11.0°）。前進追従報酬 最終 3.53。
- **分析（重要な副作用の発見）**: 足裏は水平化したが、**定格 95%超に張り付く関節が
  7/22 に増加**。`hip_yaw +20.0°`、`ankle_roll −29.7°` と脚が大きく外に開いたまま保持され、
  それらが時間の 98〜99% トルク飽和していた。さらに詳細計測で、後脚の主動力である
  **膝がほとんど動いていない**ことが判明（次で解明）。
- **次への課題**: 「関節が動かない・ぎこちない」の根本原因を突き止め、トルク飽和を下げる。

### 4.8 v8 — 膝の解放と脚の横開き抑制（最新・Sim2Real 最有力）

- **動機**: v7 の診断で 2 つの根本問題が判明。
  1. **膝ロック**: `knee_pitch` の初期値 0.10 rad は機械的下限 0.0 の**わずか 5.7° 上**。
     膝は片方向にしか曲がらないため、主動力の膝がハードストップに押し付けられ、
     **可動幅わずか 6.4° なのにトルク張り付き 100%**。これが「関節が動かない・
     ぎこちない」の正体だった。
  2. **脚の横開き**: `hip_yaw ±20°`, `ankle_roll ±30°` で開いたまま保持され、その 4 関節が
     定格 95%超に時間の 98〜99% 張り付き（発熱・脱調リスク）。
- **実装**:
  - **初期姿勢の再導出**（足裏水平条件 hip+knee+ankle=−1.5708 を維持したまま膝に可動余地）:
    `knee_pitch 0.10→0.40`（下限までの余裕 5.7°→22.9°）、`hip_pitch −1.671→−1.770`、
    `ankle_pitch 0.0→−0.201`、`shoulder_pitch −1.5708→−1.771`、`elbow_pitch 0.0→0.20`
    （後脚が短くなる分、前脚も曲げてリーチを一致。前後差 3.0mm→2.0mm）、
    `base_init_pos z 0.197→0.1946`、`base_height_target` も同値。
  - 環境を **`khr_quad_env5.py`** に変更し、`_reward_hip_pos` の罰対象に
    `hip_yaw` / `ankle_roll` を追加（横開きをさらに抑制）。
  - **静的検証**: 新姿勢は 400 step で base_z 0.193 維持・x ずれ 1.7mm と安定。
    静止保持時のトルク定格 95%超は 0/22（膝はわずか 7〜8%）。
- **結果（v7 → v8）**:

  | 指標 | v7 | v8 |
  |---|---|---|
  | 膝の可動幅 | 6.4° | **22.3°**（3.5 倍） |
  | 膝のトルク張り付き | 100% | **3%** |
  | 定格 95%超に張り付く関節数 | 7/22 | **2/22**（v4 水準まで回復） |
  | `hip_yaw` のズレ | +20.0° | **+1.7°** |
  | `ankle_roll` のズレ | −29.7° | **−5.3°** |
  | 足裏の傾き | ≈11.0° | **7.5°（全版で最良）** |
  | 前進追従 | 95% | 92% |
  | 横追従 | 58% | 62% |
  | 旋回追従 | 85% | 91% |
  | 後方 push 耐性 | 1.5 | **2.5+ m/s** |
  | 横 push 耐性 (+y/−y) | 1.5 | 1.2 m/s |

- **分析**: 主目的（膝の解放・トルク飽和の低減）を達成。実機で最も効くのは
  **トルク飽和の解消（7/22→2/22）**で、発熱・脱調という実害を直接下げる。動作の自然さ・
  足裏接地・旋回性能も最良。**トレードオフ**として横 push 耐性が 1.5→1.2 に低下したが、
  これは v7 の「脚を大きく開いて踏ん張る姿勢」（＝トルク飽和の原因そのもの）を
  正常化した代償であり想定内。
- **総合評価**: **v8 が現時点で Sim2Real 用の最良モデル**。

### 4.9 v8 の 6 方向動作記録（前後・左右・左右旋回）

`model_3999` を各方向 10 秒、ノイズ・DR 無効で録画・実測（`experiments/khr-quadruped8/videos.md`）。
速度は loco frame で評価（base 座標のままだと旋回が −4% と誤って出る点に注意）。

| 動画 | 指令 (vx, vy, wz) | 実測 (vx, vy, wz) | 追従率 |
|---|---|---|---|
| `walk_v8_forward.mp4`    | (+0.40, 0, 0) | (+0.347, +0.000, +0.066) | 87% |
| `walk_v8_backward.mp4`   | (−0.25, 0, 0) | (−0.019, −0.019, −0.003) | **7%** |
| `walk_v8_left.mp4`       | (0, +0.15, 0) | (+0.036, +0.095, +0.053) | 63% |
| `walk_v8_right.mp4`      | (0, −0.15, 0) | (+0.032, −0.138, +0.017) | 92% |
| `walk_v8_turn_left.mp4`  | (0, 0, +0.50) | (+0.034, +0.005, +0.454) | 91% |
| `walk_v8_turn_right.mp4` | (0, 0, −0.50) | (+0.026, −0.012, −0.503) | 101% |

- **後退が動かないのはバグではなく学習範囲外**: v8 の `lin_vel_x_range=[0.0, 0.4]` は
  負の vx を一度も経験していない。後退動画は完全な未学習領域(OOD)で、方策は
  「転ばず静止」を選ぶ（実測 −0.019 m/s ≒ 静止、転倒せず）。後退が必要なら
  範囲を `[-0.2, 0.4]` 等に広げて再学習が要る（前進性能とのトレードオフに注意）。
- **横移動が左右非対称**（左 63% / 右 92%）: 左横では指令外の前進 +0.036、旋回 +0.053 が
  乗り弧を描く。原因候補は (a) URDF/初期姿勢の左右非対称、(b) 横移動報酬の左右不均等学習。
  横方向は v4 以降ずっと弱く、最大の改善余地。
- **旋回は左右とも良好**（左 91% / 右 101%）。v7 の 85% から改善し左右対称性も良い。

### 4.10 v8 の実機動作確認と教授フィードバック

v8 を実機 KHR-3HV で動作させ、教授に確認いただいた（研究の方向性・進捗とも良好との評価）。
一方で 3 つの指摘を受けた: **① 動きがぎこちない、② モーターのトルクが最大になっている可能性、
③ 後退ができない**。以後はシミュ上の四足歩行精度の向上に軸足を移す。

実測で診断した結果（v8, ノイズ/DR無効）、①②の根本原因が判明した:

- **後脚 l/r_hip_pitch が全歩行モードで常時トルク100%飽和**し、PD目標角から
  **50〜66度ズレ続ける**（＝指令位置に到達できない bang-bang 制御）。他20関節は1〜4°で正常追従。
- **静止保持なら同姿勢を43%トルクで支えられ、定格95%超は0/22**。つまりハードが弱いのではなく、
  **方策がアクチュエータ飽和を悪用した歩容を学習していた**（報酬が飽和をほぼ罰していなかった）。
- ③ は `lin_vel_x_range=[0,0.4]` で負の速度を一度も学習していなかったため（バグではない）。

この診断が v9 の設計根拠。詳細は `experiments/khr-quadruped9/investigation.md`。

### 4.11 v9 — トルク飽和対策と後退の追加（実機フィードバック反映）

- **動機**: §4.10 の3指摘。根本原因は hip_pitch のトルク飽和・到達不能指令。
- **実装**（環境 `khr_quad_env6.py`、姿勢は v8 のまま。変更を報酬とコマンド範囲に限定）:
  - **[A] `_reward_torque_limits`（-2.0）**: 定格(1.373Nm)の85%超のトルクを罰し飽和を抑制（補助）。
    effort limit は `get_dofs_force_range` で取得。
  - **[B] `_reward_dof_pos_error`（-1.0, 主対策）**: PD目標角と実角の誤差を罰し、到達不能な
    指令＝飽和の直接原因を叩く。`step` で `self.target_dof_pos` を保持して参照。
  - **[D] `acceleration` -0.00002→-0.00004**: カクつきの補助的低減。
  - **[E] `lin_vel_x_range` [0,0.4]→[-0.2,0.4]**: 後退を開放。
- **結果（v8 → v9）**:

  | 指標 | v8 | v9 |
  |---|---|---|
  | hip_pitch 追従誤差（前進） | 47〜60° | **2.6〜8.0°** |
  | 飽和関節数 (duty>90%) | 2/22 (l/r_hip_pitch) | **1/22 (r_hip_pitch のみ)** |
  | 平均 \|τ\|/limit（前進/横/旋回） | 36/30/28% | **28/18/19%** |
  | \|Δaction\|RMS（前進/横/旋回） | 0.198/0.119/0.094 | **0.137/0.057/0.046** |
  | dof_vel RMS（前進） | 1.175 | **1.018 rad/s** |
  | 後退追従 | 7%（未学習） | **74%** |

- **6方向追従率（v8→v9）**: 前進 87→75%、後退 7→**74%**、左横 63→67%、右横 92→62%、
  左旋回 91→**100%**、右旋回 101→85%。前進・右側の低下は「飽和を悪用した速さ」を止めた代償で、
  sim2real的にはむしろ健全（実機で出せる範囲の歩容）。歩容崩壊・転倒はない。
- **評価**: ①改善（追従誤差55°→8°・滑らかさ全方向で低下）、②大幅改善だが右hip_pitchは
  100%負荷が残存（左右非対称・トロットの2脚支持による右後脚荷重）、③解決。
- **残課題（v10候補）**: 右 hip_pitch の飽和（左右非対称）。候補C（hip系の action_scale/kp を
  絞る）、URDF/初期姿勢の左右対称チェック、支持脚が増える歩容（クロール）、base高さ/速度の
  引き下げによる動的荷重の低減。詳細は `experiments/khr-quadruped9/results.md`。

### 4.12 v10 — 左右対称化と低負荷化（トルク100%飽和の完全解消）

- **動機**: v9 の実機安全性フィードバック（**① トルク100%はモーター焼損リスクで絶対NG・
  負荷は低く、② 前進なのに横へ逸れる、③ カクッとする**）。実測診断で、前進時に
  **右 hip_pitch だけ平均100%張り付き・左は56%＝左右非対称な歩容**が判明。前脚(腕)は
  平均22%で余裕、横速度 +0.030/yaw +0.028 のドリフト。**右hip飽和と横逸れは「片脚に荷重を
  預ける非対称歩容」という同一原因**だった。
- **実装**（環境 `khr_quad_env7.py`、姿勢・トロットは v8/v9 のまま。報酬側で対症。承認方針=
  「報酬で対称化＋減速」）:
  - **`_reward_leg_load_balance`（-1.0, 主対策）**: 左右後脚の平均トルク負荷(EMA, α=0.02)の差を
    罰す。トロットは左右が半周期ずれ瞬時比較不可のため時間平均で測る。
  - **`_reward_drift`（-10.0）**: 指令からの横速度/旋回のズレを二乗で罰し直進性を補助。
  - **`_reward_action_smoothness2`（-0.01）**: 行動の2階差分（躍度）を罰しカクつきを低減。
  - **`torque_limits` 強化**: soft比 0.85→0.65、scale −2.0→−5.0（100%張り付きを禁止）。
  - **前進上限 0.4→0.3**（推進トルクのピークを下げ低負荷化）。後退 −0.2 は維持。
- **結果（v9 → v10）**:

  | 指標 | v9 | v10 |
  |---|---|---|
  | **飽和関節数 (duty>90%)** 全方向 | 1/22 | **0/22** |
  | 平均 \|τ\|/limit（前進/後退/横/旋回） | 28/21/18/19% | **17/12/11/11%** |
  | r_hip_pitch 平均負荷（前進） | 100% | **23%** |
  | l/r_hip_pitch 負荷（前進, 対称性） | 56/100 | **32/23（均衡）** |
  | 横移動追従 左/右 | 67/62 | **60/59（対称）** |
  | 前進時 横ドリフト vy | +0.032 | **−0.004** |
  | 前進時 yaw ドリフト | +0.028 | +0.053（**残**） |
  | \|Δaction\|RMS（前進） | 0.137 | **0.088** |
  | dof_vel RMS（前進） | 1.018 | **0.660 rad/s** |

- **6方向追従率（v10）**: 前進 71%(実速0.213)、後退 55%、左横 60%、右横 59%、左旋回 108%、
  右旋回 113%。並進追従の低下は「トルク上限強化＋前進上限0.3」による意図的トレードオフ
  （安全・低負荷優先）。旋回は左右対称・良好。
- **評価**: **① 完全達成**（全方向 0/22 飽和・平均負荷11〜17%＝焼損リスク除去）、
  **② 大幅改善**（横ドリフト解消・横移動が左右対称。ただし前進時 yaw は残存）、
  **③ 改善**（ガタつき・関節速度とも低下）。
- **残課題（v11候補）**: 前進時の yaw ドリフト（+0.053）。候補: drift の yaw 成分強化、
  heading の明示的追従、leg_load_balance への hip_yaw/前脚の取り込み。並進追従の回復も。
  詳細は `experiments/khr-quadruped10/results.md`。

### 4.13 v11 — ❌ 失敗: 瞬時 yaw 罰で並進が崩壊（重要な失敗記録）

- **動機**: v10 の残課題＝前進時の yaw ドリフト（約3°/秒、10秒で27°、旋回半径4m、直進1.7mで
  横ずれ0.30m）。Sim2Real 前の直進性向上として対処する狙いだった。
- **実装（1点のみ）**: `_reward_drift` の yaw 成分を横の**5倍**に重み付け（`yaw_drift_weight=5.0`）。
- **結果: 並進が崩壊**。前進28%/後退0%/横0%（旋回のみ85〜92%生存）。`tracking_lin_vel` は
  3.56→0.318。前進の軌跡は正味変位ほぼ0＝「その場で揺れるだけ」。
- **原因**: トロットは前進時に**本来 yaw が周期的に揺れる**。これを5倍で強く罰したため
  「並進すると罰される→動かない方が得」という degenerate 解に収束。旋回だけ残ったのは
  指令旋回時は `wz_err≈0` で罰がかからないため。`tracking_lin_vel` が exp 罰で低速でも
  報酬が残ることも後押しした。
- **学び（→ v12 の設計指針）**: 直進性は「**瞬時 yaw**」ではなく「**持続的なヘディング偏り
  （時間平均）**」で罰すべき。瞬時罰は歩容本来の周期的揺れまで潰す。
- v11 は不採用。詳細は `experiments/khr-quadruped11/results.md`。

### 4.14 v12 — yaw ドリフトを EMA で解消（並進を保ったまま約半減）

- **動機**: v11 の失敗を修正し、並進を潰さずに yaw ドリフトを下げる。
- **実装**（環境 `khr_quad_env9.py`）:
  - **`_reward_heading_drift`（-40.0）**: yaw 誤差 `wz_err = wz − cmd_wz` の **EMA（α=0.02, 約1s
    平均）の二乗**を罰す。**周期的な揺れは平均で相殺**され、じわじわ曲がる持続成分だけを叩くので
    並進を潰さない。指令旋回時は `wz_err≈0` で EMA≈0＝罰されない。
  - 瞬時 yaw 罰は v10 相当に戻す（`yaw_drift_weight` 5.0→1.0）。他報酬・姿勢・トロットは v10 のまま。
- **結果（v10 → v12）**:

  | 指標（前進） | v10 | v11(失敗) | v12 |
  |---|---|---|---|
  | 前進追従 | 71% | 28% | **68%** |
  | 前進の正味変位(8秒) | 1.70 m | 0.01 m | **1.65 m** |
  | 横ずれ | 0.30 m(18%) | — | **0.16 m(10%)** |
  | 積算ヘディング | +27°/10s | — | **+16°/10s** |
  | yaw ドリフト率 | +0.053 | — | **+0.024** |
  | 飽和関節数 | 0/22 | — | **0/22（維持）** |
  | l/r_hip_pitch 負荷 | 32/23% | — | 33/24%（対称維持） |

- **6方向追従（v12）**: 前進68/後退47/左横60/右横49/左旋回105/右旋回102%。全方向で歩行が生存。
- **評価**: **並進の維持（v11崩壊の回避）✅✅、yaw を約半減 ✅、トルク0/22・低負荷・対称の維持 ✅**。
  EMA（持続偏り）で罰す設計が奏功。yaw は完全ゼロではない（16°/10秒）が、テレオペ補正可能な水準。
- **方針**: シミュ磨きはここで一区切り。**次は Sim2Real へ pivot**（符号キャリブ→安全な v12 を
  実機投入→`obs2csv` でギャップ実測）。詳細は `experiments/khr-quadruped12/results.md`。

### 4.15 v13 — すり足の原因（クリアランス報酬の設計欠陥）を修正（🟡 部分的成功）

- **発端**: v12 の動画を見た指導教員・本人から「足回りの動きが不安」との指摘。**足回りの品質を
  初めて実測**したところ、幾何オフセットを補正した**真のクリアランス**（各足の接地基準高さからの
  持ち上げ量）が **前脚 11〜14mm / 後脚 0〜5mm（目標60mm）** しかなく、前脚は接地中に
  0.126〜0.146 m/s（前進速度 0.213 の6〜7割）で滑っていた。**歩容の実体は「すり足・引きずり」**。
  後左足は8秒間一度も地面から離れていなかった。
- **根本原因（設計欠陥）**: `_reward_feet_clearance` が `feet_height_target = 0.06` を
  **世界座標の絶対高さ**として4本の足リンクに一律適用していた。各足リンクの立位高さを実測すると

  ```
  foot_ref_z = [0.101, 0.101, 0.027, 0.027]   # [前左, 前右, 後左, 後右]
  ```

  **前脚(腕リンク)は立位で既に 0.101 m** ＝ 目標 0.06 を超えており、**上げるほど誤差が増えて
  報酬が減る＝クリアランス報酬が足上げを罰していた**。後脚も目標が遠く、v9 以降で積み増した
  トルク罰・滑らかさ罰に負けていた。**v9〜v12 を通じて見落とされていた欠陥**（トルク飽和の解消に
  注目するあまり、足上げを測っていなかった）。
- **実装**（環境 `khr_quad_env10.py`。姿勢・トロット・トルク安全策は v12 のまま）:
  - **クリアランスの相対化（主対策）**: 各足の接地時基準 `foot_ref_z` からの相対高さで評価。
    `foot_clearance_target = 0.03`（4脚とも到達可能）。scale 0.2→**1.0**。
  - **`_reward_feet_air_time`（1.0）新設**: 接地の瞬間に「滞空時間 − 0.2s」を加点し
    「上げて・運んで・置く」歩容を促す（legged_gym 定番）。状態更新は `step` 側で行い報酬は純粋。
  - **`contact_no_vel` −0.2 → −1.0**（引きずりを強く罰す）。
- **結果（v12 → v13）**:

  | 指標 | v12 | v13 |
  |---|---|---|
  | 真のクリアランス 前脚 | 0.011 / 0.014 m | **0.032 / 0.031 m（約3倍・目標達成）** |
  | 真のクリアランス 後脚 | 0.000 / 0.005 m | 0.005 / 0.006 m（**未達**） |
  | 後脚の歩容一致率 | 85 / 89% | **94 / 90%** |
  | 後脚の接地率 左右差 | 12.3 pt | **4.0 pt（対称化）** |
  | 前脚の接地滑り | 0.126 / 0.146 m/s | 0.169 / 0.151 m/s（**悪化**） |
  | **飽和関節数（全方向）** | 0/22 | **0/22（維持）** |
  | ピーク \|τ\|（前進） | 0.909 Nm (66%) | 0.943 Nm (69%) |
  | 追従 前進/後退/左横/右横 | 68/47/60/49% | **73/60/74/72%** |
  | \|Δaction\|RMS（前進） | 0.0875 | 0.1037（動作増に伴う悪化） |

- **評価**: **原因診断は正しく、修正は前脚で明確に効いた**（相対化により「罰」が「報酬」に転じた）。
  **全並進方向で追従も向上**（横は +20pt 超）し、**トルク飽和ゼロも維持**。一方
  **後脚は 5〜6mm のままで依然擦っており、「すり足の解消」としては道半ば**。
  副作用として前脚の着地滑りと滑らかさ指標が悪化した。
- **残課題（v14候補）**: 後脚が上がらない理由の仮説は (1) 後脚が体重の大半を支えており
  `base_height`(−10.0)/`orientation`(−5.0) の罰と衝突、(2) 後脚は膝を大きく曲げる必要があり
  `dof_pos_error`/`torque_limits` と competing、(3) 後脚の接地率が 46〜50% と長い。
  対策候補: 後脚のクリアランス目標を 0.015〜0.02m に下げる／後脚の重みを上げる、
  `base_height` 罰を緩める、`gait_period` を延ばす、着地速度罰の追加。
  詳細は `experiments/khr-quadruped13/results.md`。

### 4.16 v14 — 後脚の「変な力の入れ方」に挑戦（❌ 仮説が反証された負の結果）

- **発端**: ユーザ指摘「前脚は v12 までの上げ幅でいい。問題は後ろ足で、**変な力の入れ方をして
  歩いてる感じ**」。後脚の力学（ROM・トルク・仕事率 τ·ω）を実測して診断した。
- **診断（v13, 前進0.3）**: 4つの異常が判明。
  1. **膝がほとんど曲がらない**（ROM 9.6〜10.2°）。v8 では 22.3° あり、**v9 以降で半減**。
  2. **足首が膝より大きなトルクを負担**（30% > 19〜24%）＝**脚が棒のまま足首を支点に振られる**
     不自然な使い方。これが「変な力の入れ方」の実体。
  3. **力の 73〜85% が動きに逆らう向き（ブレーキ）**。駆動ではなく抵抗が主。
  4. **胴体の上下動がわずか 2mm**（`base_height` 罰 −10.0 は全報酬中で最大級）。
- **立てた仮説**: 胴体が垂直に固定されているため後脚を畳む余地がない → `base_height` を緩めれば
  膝が解放される。
- **実装**（環境 `khr_quad_env11.py`、変更2点）:
  - `base_height` −10.0 → **−3.0**（主対策）
  - **クリアランス目標を脚別に分離**: 前脚 0.012（**v12 水準へ戻す**）/ 後脚 0.02
    （`foot_clearance_target` をスカラ→脚別テンソル [FL,FR,RL,RR] 化）
- **結果: 仮説は反証された**。

  | 指標 | v13 | v14 |
  |---|---|---|
  | base_z 上下動 (std) | 2.0 mm | **1.8 mm（ほぼ不変）** |
  | l_knee ROM | 9.6° | **6.4°（悪化）** |
  | r_knee ROM | 10.2° | 14.8° |
  | l_ankle 負荷 | 30% | **32%（依然 最大）** |
  | 後脚クリアランス | 0.005/0.006 m | 0.008/0.007 m（目標0.02に遠い） |
  | 前脚クリアランス | 0.032/0.031 m | **0.015/0.019 m（v12水準へ復帰 ✅）** |
  | 飽和関節数 | 0/22 | **0/22（維持）** |
  | \|Δaction\|RMS（前進） | 0.1037 | **0.0959（改善）** |
  | 前進 yaw ドリフト | +0.029 | **−0.010（改善）** |

  **`base_height` 罰を 1/3 に緩めても胴体の上下動は変わらなかった**（2.0→1.8mm）。つまり
  胴体を固めていたのはこの罰ではなく、後脚の屈曲も解放されなかった。左膝はむしろ悪化し、
  左右非対称も再発した（左膝6.4° vs 右膝14.8°）。
- **評価**: 前脚を v12 水準へ戻す（脚別目標）は成功。安全性・滑らかさ・直進性は維持〜改善。
  しかし**主目的の後脚は未達**。歩容としては v13 から改善していないため、実機に載せるなら v13/v12。
- **負の結果としての価値**: 「胴体の固さ＝`base_height` 罰」という筋の通った仮説を**実測で棄却**でき、
  容疑者を絞り込めた。
- **次の仮説（v15候補）**: `_reward_feet_orientation`（−1.0, v7導入）が**接地/遊脚を区別せず常に
  足裏を水平に保たせている**（実装に contact マスクがない）。遊脚中も水平を強いられるため
  **膝を曲げられず、その維持仕事が足首に集中**している可能性が高い（足首が最大トルクという
  観測と整合）。対策は「`feet_orientation` を接地時のみに限定」。
  詳細は `experiments/khr-quadruped14/results.md`。

### 4.17 v15 — 遊脚の解放（🟡 主目的は未達だが**膝が動かない真因を特定**）

- **動機**: v14 で `base_height` 仮説が反証されたため、次の本命仮説を検証。
  目標は「犬のようにスムーズに関節を動かす歩行」。
- **仮説**: `_reward_feet_orientation`（−1.0, v7導入）に **contact マスクが無く**、接地/遊脚を
  区別せず常に足裏を水平に保たせていた。遊脚中に膝を曲げると足裏が傾いて罰される →
  **膝の屈曲が抑制**され、水平維持の仕事が **足首に集中**する。
- **実装**（環境 `khr_quad_env12.py`、変更1点）: `_reward_feet_orientation` を
  **接地時のみ**適用（`feet_orientation_stance_only: True`）。
- **結果: 仮説は部分的に正しかったが、主目的は未達**。

  | 指標 | v14 | v15 |
  |---|---|---|
  | l_ankle 負荷 | **32%** | **24%（低下＝仮説の裏付け）** |
  | l_knee ROM / r_knee ROM | 6.4° / 14.8° | 7.2° / 7.8°（**回復せず**） |
  | 後脚 足裏傾き（接地時） | 11.0° | **13.5°（悪化＝副作用）** |
  | 前進/後退/左横/右横 追従 | 72/57/66/67% | **75/61/90/69%（最良）** |
  | 後脚 歩容一致率 | 89/90% | **92/93%（最良）** |
  | 飽和関節数 / ピーク\|τ\| | 0/22 | **0/22 / 0.846Nm(62%)** |

- **【最大の収穫】真因の特定**: PD の**指令角**と**実測角**の可動域を比較したところ、

  | 関節 | 指令ROM | 実測ROM | 追従率 |
  |---|---|---|---|
  | l_knee_pitch | **6.8°** | 7.8° | 114% |
  | r_knee_pitch | **7.4°** | 8.4° | 115% |
  | l_hip_pitch | 14.2° | 15.6° | 110% |

  **膝の指令 ROM 自体が 7° 程度しかなく、実測はそれを上回っている**。
  ⇒ **トルク不足や PD の追従限界ではなく、方策が膝を動かす指令を出していない**と確定。
  原因は完全に**報酬設計**にある（`torque_limits` は容疑から外れた）。
- **なぜ膝を使わないか**: 遊脚は股関節の回転だけで振られており（hip ROM 15°）、
  **膝の屈曲を直接求める報酬が存在しない**。一方で関節運動には広くコストがかかる
  （`dof_vel`/`action_rate`/`action_smoothness2`/`acceleration`/`dof_pos_error`）。
  「**膝を曲げる利益がなく、動かすコストだけある**」ため、膝を固めて股関節だけで振る戦略に収束した。
  これは犬・馬が遊脚期に膝を曲げて脚を短縮するのと正反対。
- **副作用**: 遊脚中の足裏を自由にした代償で、**接地時の足裏の傾きが 11.0° → 13.5° に悪化**
  （v7 で対処した「足の縁で歩く」問題の軽度な再発）。
- **次（v16候補）**: 真因が「膝を曲げる利益がない」ことなので、**遊脚期の膝屈曲を直接報酬にする**
  のが本命。副次的に、関節運動コストの緩和や `feet_orientation` の中間案（接地＋着地直前）も候補。
  詳細は `experiments/khr-quadruped15/results.md`。

### 4.18 v16 — 遊脚期の膝屈曲報酬（❌ 報酬ハック：膝を「曲げっぱなし」にされた）

- **動機**: v15 で「膝を動かす**指令自体が出ていない**＝報酬起因」と真因が確定したため、
  **遊脚期の膝屈曲を直接報酬にする**（犬・馬が遊脚期に脚を短縮する原理）。
- **実装**（環境 `khr_quad_env13.py`、変更1点）: `_reward_knee_flexion`（scale 2.0）を新設。
  後脚の膝が遊脚期に default(0.40rad) より曲がっているほど加点（目標 +0.30rad、sharpness 10）。
  接地期は対象外。
- **結果: 報酬ハックが発生し失敗**。学習ログでは `rew_knee_flexion = 1.4803` と高得点なのに、
  **膝 ROM はむしろ縮小**（v15 7.2/7.8° → **2.7/2.8°**）。原因を膝角度の分布で特定した:

  | 膝 | 平均角度 | swing平均 | stance平均 | **swing−stance差** | 実測ROM |
  |---|---|---|---|---|---|
  | l_knee | 0.685 rad | 0.700 | 0.673 | **+1.5°** | 2.7° |
  | r_knee | **0.700 rad** | 0.703 | 0.697 | **+0.3°** | 2.8° |

  （default 0.40 rad、報酬の目標 0.70 rad）
  **膝は常時 0.70 rad ＝報酬の目標角度で固定**されており、「曲げて伸ばす」動作をしていない。
- **原因（設計の欠陥）**: 報酬が **膝の「位置（絶対的な屈曲量）」** を見ていた。
  この定義では「常に目標角度で固定する」だけで満点が取れ、関節を動かす必要がない。
  むしろ動かすと目標から外れて損をするうえ `dof_vel`/`action_rate` のコストもかかるため、
  **固定が最適解**になった。「膝を曲げる」ことは報酬にしたが、
  **「膝を曲げ伸ばしする（動かす）」ことは報酬にしていなかった**。
- **副作用**: 接地時の足裏傾きが 13.5° → **16.1°** に悪化、後脚の接地率左右差も 1.0 → **10.8pt** に後退。
- **維持できたもの**: 飽和 0/22・ピーク63%、前進追従75%、後退は61→69%に改善。
- **学び（一般的な教訓）**: **「望む状態」ではなく「望む変化」を報酬にすべき**。
  位置を報酬にすると、その位置で静止するのが最適解になる。周期運動を促すなら
  **位相間の差分・速度・基準からの相対量**を報酬にする必要がある。
  これは v13 で `feet_clearance` を「絶対高さ」から「接地基準からの相対高さ」へ直したのと
  **同じ構造の誤り**であり、報酬設計の一般則として卒論に記載する価値がある。
- **次（v17候補）**: 膝の **swing−stance 差**を報酬にする（接地時の膝角度の EMA を基準に、
  遊脚中に追加で曲がった分だけを加点＝定角度で固定すると差がゼロになりハックできない）。
  v13 の `foot_ref_z`（接地基準）と同じ発想。併せて接地の質（足裏傾き16.1°）への対処も要。
  詳細は `experiments/khr-quadruped16/results.md`。

### 4.19 v17 — 膝の「swing−stance 差」を報酬に（✅主目的達成／❌代償大）

- **動機**: v16 の報酬ハック（膝を目標角度で固定）を修正する。これまでの傾向から、
  **v13 の成功（絶対高さ→接地基準の相対量）と v16 の失敗（膝の絶対角度）は同じ構造**と判断し、
  同じ設計則を膝に適用した。
- **実装**（環境 `khr_quad_env14.py`、変更1点。v16 の報酬は削除して置換）:
  - `_reward_knee_swing_flexion`（scale **3.0**）: 基準を **その脚自身の「接地時の膝角度」(EMA,
    α=0.05、接地相のみ更新)** に取り、**遊脚中にそこから追加で曲がった分**だけを加点
    （目標 +0.25rad、飽和線形）。**定角度で固定すると差がゼロ＝報酬0** になるためハック不可。
  - 学習前スモークで耐性を実証: 0.70rad で固定 → 報酬 **0.0000**／接地基準から +0.25rad → **2.0000**。
- **結果: 膝が周期運動を始めた（主目的達成）**。

  | 指標 | v15 | v16(ハック) | **v17** |
  |---|---|---|---|
  | l/r_knee ROM | 7.2 / 7.8° | 2.7 / 2.8° | **56.2 / 54.7°** |
  | 膝 swing−stance 差 | — | +1.5 / +0.3° | **+12.6 / +13.8°** |
  | l/r_knee 負荷 | 17 / 15% | 8 / 9% | **33 / 38%** |
  | l/r_ankle 負荷 | 24 / 22% | — | **14 / 18%** |
  | 後脚クリアランス | 0.010/0.007 | 0.008/0.011 | **0.016/0.015 m** |
  | 胴体の上下動 (std) | — | — | 1.8 → **6.6 mm** |

  **膝が足首を上回る主動力に戻り**（33-38% > 14-18%）、v13 以来の「脚が棒のまま足首を支点に
  振られる」状態が解消。後脚クリアランスも目標 0.02m に接近した。
- **代償（実機観点で無視できない）**:
  - **トルクのピークが 63% → 91%（r_knee）** に上昇。持続的な飽和(duty)は 0/22 のままだが、
    「100%は絶対NG」の要件に対し余裕が 9% しかない。**要対処**。
  - **追従が全並進方向で低下**: 前進 75→**62%**、後退 69→**49%**、横 62/65→**48/48%**（旋回は維持）。
  - **接地の質が更に悪化**: 後脚 足裏傾き 16.1 → **21.4°**（v14 比で約2倍）。接地滑りも増加。
    v15 で先送りした課題が無視できない水準に。
  - **絶対姿勢のドリフト**: 膝の平均角度が 1.5rad（default 0.40）、base_z 0.179m（目標 0.195）。
- **学び**: **差分報酬はハックを防ぐが、絶対量は無拘束になる**。
  v16（絶対量→固定される）と v17（差分→絶対値がドリフト）は表裏の関係であり、実用的には
  **「差分で動きを促し、絶対量は別途ゆるく拘束する」**組み合わせが要る。
  また、望む挙動が得られた直後に別指標（トルク・接地・追従）が悪化するのは本研究を通じた
  一貫した傾向で、**常に多指標で確認する**必要がある。
- **評価**: 設計課題としては解決。ただし代償が大きく**このままでは実機に載せられない**。
  実機候補は依然 **v15**（追従・対称性・滑らかさが最良）。
- **次（v18候補）**: ①`knee_swing_flexion` を 3.0→1.5〜2.0 に下げ ROM を適正化（v8 の 22° が目安）、
  ②接地の質の回復（`feet_orientation` を「接地時＋着地直前」へ）、③絶対姿勢の緩い拘束。
  詳細は `experiments/khr-quadruped17/results.md`。

---

## 5. 全バージョン横断の実測比較

### 5.1 開発の流れ（1 行要約）

| 版 | exp | 環境ファイル | 一言 | 前進追従報酬(最終) |
|---|---|---|---|---|
| v1 | khr-quadruped  | khr_quad_env  | 初の四足歩行成功（遅い） | 1.45 |
| v2 | khr-quadruped2 | khr_quad_env  | 速く狙って逆に遅化（診断材料） | 1.12 |
| v3 | khr-quadruped3 | khr_quad_env  | 報酬バランス是正でブレークスルー | 3.89 |
| v4 | khr-quadruped4 | khr_quad_env  | 全方向化（横・旋回追加）＝ベースライン | 3.91 |
| v5 | khr-quadruped5 | khr_quad_env2 | push 外乱（頑健↔性能トレードオフ） | 3.24 |
| v6 | khr-quadruped6 | khr_quad_env3 | push カリキュラムで両立 | 3.49 |
| v7 | khr-quadruped7 | khr_quad_env4 | 足裏水平化（副作用でトルク飽和増） | 3.53 |
| v8 | khr-quadruped8 | khr_quad_env5 | 膝の解放＋横開き抑制 | 3.52 |
| v9 | khr-quadruped9 | khr_quad_env6 | トルク飽和対策＋後退追加（実機FB反映） | 3.48 |
| v10 | khr-quadruped10 | khr_quad_env7 | 左右対称化＋減速でトルク100%完全解消・低負荷化 | 3.56 |
| v11 | khr-quadruped11 | khr_quad_env8 | ❌失敗: 瞬時yaw罰(×5)で並進崩壊（前進28%/後退0%） | 0.32 |
| v12 | khr-quadruped12 | khr_quad_env9 | yaw誤差EMAで直進化（並進保持でyaw約半減） | 3.5+ |
| v13 | khr-quadruped13 | khr_quad_env10 | すり足の原因(絶対高さ目標)を修正。前脚は3倍改善・後脚は未達 | 3.5+ |
| v14 | khr-quadruped14 | khr_quad_env11 | ❌base_height仮説は反証。前脚はv12水準へ復帰、後脚は未達 | 3.5+ |
| v15 | khr-quadruped15 | khr_quad_env12 | 足裏水平罰を接地時のみに。足首負荷↓・追従最良だが膝は未回復。**真因特定** | 3.5+ |
| v16 | khr-quadruped16 | khr_quad_env13 | ❌膝屈曲報酬が報酬ハックされ「曲げっぱなし」に（膝ROM 2.7°） | 3.5+ |
| v17 | khr-quadruped17 | khr_quad_env14 | ✅膝が周期運動（ROM 56°・膝が主動力）／❌ピーク91%・追従低下・接地悪化 | 3.4+ |

### 5.2 姿勢・トルク健全性の推移（後脚）

| 指標 | v4 | v5 | v6 | v7 | v8 | v9 | v10 |
|---|---|---|---|---|---|---|---|
| 足裏の傾き [°] | 12.4 | 22.7 | 24.3 | ≈11.0 | **7.5** | — | — |
| `ankle_pitch` [°] | −6.3 | −15.4 | −34.4 | — | — | — | — |
| `ankle_roll` [°] | −12.6 | −29.3 | −29.7 | −29.7 | **−5.3** | — | — |
| `hip_yaw` [°] | +6.0 | +18.9 | +20.0 | +20.0 | **+1.7** | — | — |
| 膝の可動幅 [°] | — | — | — | 6.4 | **22.3** | — | — |
| 飽和関節数 (duty>90%) | 2/22 | — | — | 7/22 | 2/22 | 1/22 | **0/22** |
| hip_pitch 追従誤差(前進) [°] | — | — | — | — | 47〜60 | 2.6〜8.0 | **<2** |
| 平均 \|τ\|/limit 前進 [%] | — | — | — | — | 36 | 28 | **17** |
| r_hip_pitch 平均負荷(前進) [%] | — | — | — | — | 100 | 100 | **23** |

（`—` は当該版で未計測。空欄は該当なし。前進追従報酬は各 `report.md` の最終値、
姿勢・トルクは各版評価時の実測。）

---

## 6. 重要な失敗と学び（トラブルシューティング）

研究上、失敗の記録は再現防止と考察の一次資料になるため明示的に残す。

### 6.1 探索の早期崩壊（v1）
最初の run はエピソード長 12.5 のまま学習が崩壊。`init_std` が高すぎて探索 std が
早期に潰れていた。`init_std 1.0→0.5`, `action_scale→0.15`, `entropy_coef→0.01` で回避。
**教訓**: 「学習が全く進まない」ときはまず探索量（std）の推移を疑う。

### 6.2 後ろ倒れ四足になった理由（初期姿勢）
最初の試作は胴体ピッチ −90°＝**後ろに倒れる**四足だった。KHR には首（ピッチ）関節が
無く、頭を前に向けられないため、頭を上に保つと自然に後傾解になっていた。見た目・心理面
（「後ろ倒れはホラー」）から**前倒れ（+90°）に変更**。機能上の問題ではなく設計判断。

### 6.3 「速くしようとして遅くなる」報酬設計の罠（v2→v3）
追従判定を厳しくする（sigma を下げる）だけでは速くならない。**静止でも追従報酬の大半が
得られ、動くと罰が勝つ**なら静止が最適解になる。解決は「厳しさ」ではなく
「歩く報酬 > 歩く罰」に**バランスを組み替える**こと（tracking を上げ、動作系の罰を下げる）。

### 6.4 頑健性↔性能のトレードオフ（v5→v6）
強い外乱を最初から与えると方策が守りに入り歩行が鈍る。**カリキュラム**（まず歩行、
その後で外乱漸増）で両立。legged_gym の定番と整合。

### 6.5 報酬の副作用の連鎖（v6→v7→v8）
足裏を水平化する報酬（v7）を足したら、別の関節（hip_yaw/ankle_roll）が飽和した。
さらに調べると膝が下限にロックしていた（v8 で解明）。**一つの報酬追加が別の関節に
しわ寄せする**ため、単一指標でなく「全 22 関節のトルク duty・可動幅」を横断で監視する
必要がある。これが v8 の詳細計測（膝 ROM・duty）につながった。

### 6.6 膝の片方向可動域の見落とし（v8）
`knee_pitch` の可動域 [0.0, 2.793] は片方向。初期値 0.10 は下限の 5.7° 上に過ぎず、
主動力の膝が事実上ロックしていた。**URDF の joint limit を初期姿勢設計に織り込む**べきだった。

### 6.7 計測時の座標系ミス（v8 6 方向計測）
旋回速度を base 座標系で測ると −4% と誤って出た。胴体 90° ピッチのため
**`loco_ang_vel` を使う**必要がある（§2.1）。報酬計算だけでなく評価計測でも同じ補正が要る。

### 6.8 GPU の競合（運用上の教訓）
録画（GPU）を学習中に実行し、学習を落としたことがある。**学習中に GPU 作業を重ねない**。

### 6.9 実機ネットワークの誤診（記録）
UDP タイムアウトの原因を最初 ufw と推測したが、`ufw status` は非アクティブ＝遮断していない
（無実）。真因は **PC の IP が DHCP なのに ESP32 の `keys.h` が送信先 IP を固定**していたこと。
実機フェーズで PC の IP 固定（DHCP 予約）が必要。

---

## 7. 残課題と今後の方針

### 7.1 シミュレーション内の残課題
- **横移動の左右非対称**（左 63% / 右 92%）: 最大の改善余地。URDF 非対称か報酬の
  左右不均等が疑い。
- **横 push 耐性の回復**（1.2→1.5）: v8 で正常化と引き換えに低下。踏ん張りに頼らない
  形での回復が課題。
- **後退の未対応**: 必要なら `lin_vel_x_range` を負に拡張して再学習。

### 7.2 Sim2Real（最終目標）に向けて
- 実機ファイル（`Meridian_consoleS_quad.py` / `khr_quad_env_no_gs.py`）はまだ
  バージョン管理外。fork 元 `s-kajita/Meridian_consoleS` には push しないため、
  管理方針（`khr_rl_tora` 配下へ移す等）の決定が必要。
- **符号キャリブレーション**（`genesis2khr_dir` が全 +1 で未校正）、
  **腕の荷重負担検証**、**PC の静的 IP 設定**が実機動作の前提ブロッカー。
- サーボ対応: L 系統 Meridim index = 21 + ID×2、R 系統 = 51 + ID×2。ポリシー 50Hz /
  Meridim 100Hz。

---

## 8. 付録

### 8.1 初期姿勢パラメータ（左脚。右も対称に同値）

| 関節 | v1〜v7 | v8 | 可動域 [rad] |
|---|---|---|---|
| `shoulder_pitch`（前脚） | −1.5708 | −1.771 | — |
| `elbow_pitch`（前脚） | 0.0 | 0.20 | — |
| `hip_pitch`（後脚） | −1.671 | −1.770 | [−1.920, 1.745] |
| `knee_pitch`（後脚） | 0.10 | 0.40 | **[0.000, 2.793]（片方向）** |
| `ankle_pitch`（後脚） | 0.0 | −0.201 | [−1.571, 1.745] |
| `base_init_pos` z | 0.197 | 0.1946 | — |
| `base_init_quat` | [0.7071, 0, 0.7071, 0]（y 軸 +90°） | 同左 | — |

足裏水平の幾何条件: `hip_pitch + knee_pitch + ankle_pitch = −1.5708`（両版で成立）。

### 8.2 主要ファイル一覧

| ファイル | 役割 |
|---|---|
| `khr_quad_env.py` … `khr_quad_env5.py` | 四足環境（v1→v8。番号が上がるほど機能追加） |
| `khr_train_quad.py` … `khr_train_quad8.py` | 学習スクリプト（設定のハードコード＋冒頭に版間 changelog） |
| `khr_quad_eval.py` | 学習済みポリシーのビューア再生（CPU） |
| `khr_quad_record.py` / `khr_quad_record5.py` | MP4 録画（無印=v1 env、5=v8 env） |
| `khr_quad_report.py` | `experiments/<exp>/` に report.md + metrics.csv を自動生成 |
| `quad_pose_test.py` | 初期姿勢の試作・静的安定性検証ツール |
| `experiments/khr-quadruped*/` | 各版の research record（report.md / metrics.csv / videos.md） |
| `Meridian_consoleS/Meridian_consoleS_quad.py` | 実機デプロイ（22 関節、**未コミット・要管理方針**） |
| `Meridian_consoleS/khr_quad_env_no_gs.py` | 実機用の Genesis 非依存アダプタ（**同上**） |

### 8.3 環境ファイルとバージョンの対応（何が追加されたか）

| 環境ファイル | 使う版 | 追加要素 |
|---|---|---|
| `khr_quad_env.py`  | v1〜v4 | 四足の基本（loco frame, 121 obs, トロット, DR/ノイズ/遅延） |
| `khr_quad_env2.py` | v5 | `_push_robots()`（push 外乱） |
| `khr_quad_env3.py` | v6 | push カリキュラム（`push_scale` 漸増） |
| `khr_quad_env4.py` | v7 | `_reward_feet_orientation`（足裏水平） |
| `khr_quad_env5.py` | v8 | `_reward_hip_pos` に hip_yaw/ankle_roll を追加（横開き抑制） |
| `khr_quad_env6.py` | v9 | `_reward_torque_limits` / `_reward_dof_pos_error`（トルク飽和・到達不能指令の罰）、effort limit 取得、`target_dof_pos` 保持 |
| `khr_quad_env7.py` | v10 | `_reward_leg_load_balance`（左右負荷EMA差）/ `_reward_drift`（横逸れ）/ `_reward_action_smoothness2`（躍度）、torque キャッシュ・`last_last_actions` |
| `khr_quad_env8.py` | v11 | `_reward_drift` に `yaw_drift_weight`（瞬時yaw重み付け）※失敗版 |
| `khr_quad_env9.py` | v12 | `_reward_heading_drift`（yaw誤差EMA=持続ヘディング偏り）、`wz_err_ema` |
| `khr_quad_env10.py` | v13 | `feet_clearance` を接地基準からの相対量に修正、`_reward_feet_air_time`、`foot_ref_z` |
| `khr_quad_env11.py` | v14 | `foot_clearance_target` を脚別テンソル化（前脚/後脚で別目標） |
| `khr_quad_env12.py` | v15 | `_reward_feet_orientation` を接地時のみに限定（`feet_orientation_stance_only`） |
| `khr_quad_env13.py` | v16 | `_reward_knee_flexion`（遊脚期の膝屈曲）※位置ベースでハックされ失敗 |
| `khr_quad_env14.py` | v17 | `_reward_knee_swing_flexion`（膝の swing−stance 差）、`knee_stance_ema` |

### 8.4 固定ハイパーパラメータ（全版共通）

`num_envs=4096`, `seed=1`, PPO `clip=0.2`, `desired_kl=0.01`, `entropy_coef=0.01`,
`gamma=0.99`, `lam=0.95`, `lr=1e-3`（adaptive）, `max_grad_norm=1.0`,
network `[128,64,32]` ELU, `init_std=0.5`, `kp=25 / kd=0.5`, `action_scale=0.15`,
`dt=0.02`（50Hz）, `episode_length_s=20`（max 1001 step）,
termination pitch/roll/height = 50/50/0.1。

---

*本ファイルは v1〜v17 の開発を対象とする。以降のバージョンや実機転移の記録は追記で管理する。*
